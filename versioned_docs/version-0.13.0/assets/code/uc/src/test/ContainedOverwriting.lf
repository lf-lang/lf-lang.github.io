target uC
import Overwriting from "../Overwriting.lf";
@platform("Native")
@timeout(1 sec)
main reactor {
    s = new Overwriting();
    reaction(s.y) {=
        printf("At logical time %lld, s = %d.\n",
            env->get_elapsed_logical_time(env), s.y->value
        );
    =}
}
