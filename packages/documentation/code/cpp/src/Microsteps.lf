target Cpp;
main reactor {
    state count:int(1);
    logical action a;
    reaction(startup, a) {=
        std::cout << count << " Logical time is " << get_logical_time() << std::endl;
        if (count++ < 5) {
            a.schedule(0ms);
        }
    =}
}
