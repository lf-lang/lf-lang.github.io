target C;
reactor Child (
    bank_index:int(0)
) {
    reaction(startup) {=
        info_print("My bank index: %d.", self->bank_index);
    =}
}
reactor Parent (
    bank_index:int(0)
) {
    c = new[2] Child();
}
main reactor {
    p = new[2] Parent();
}
