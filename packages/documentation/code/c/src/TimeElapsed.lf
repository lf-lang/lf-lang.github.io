target C;
main reactor TimeElapsed {
    timer t(0, 1 sec);
    reaction(t) {=
        info_print(
            "Elapsed logical time is %lld.",
            lf_time_logical_elapsed()
        );
    =}
}
