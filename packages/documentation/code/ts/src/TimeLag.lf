target TypeScript;
main reactor TimeLag {
    timer t(0, 1 sec);
    reaction(t) {=
        const t = this.util.getElapsedLogicalTime();
        const T = this.util.getElapsedPhysicalTime();
        console.log(`Elapsed logical time: ${t}, physical time: ${T}, lag: ${T.subtract(t)}`);
    =}
}